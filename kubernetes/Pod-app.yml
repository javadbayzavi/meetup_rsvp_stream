apiVersion: v1
kind: Pod
metadata:
  name: RSVP-TestApp
spec:
  containers:
  -  name: zookeeper
      image: docker.io/bitnami/zookeeper:3.8
      ports:
      - "2181:2181"
      volumes:
      - "zookeeper_data:/rsvpSample"
      environment:
      - ALLOW_ANONYMOUS_LOGIN=yes

  